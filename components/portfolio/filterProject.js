async function filterProject({param,data}){
    let response=[];
    console.log(param,data);
    // Promise.all(data.map(p => {
    //     param.technology.forEach( t => {
    //         if(response.forEach(i => i.id).indexOf(p.id)===-1){
    //             if(p.technology.includes(t)){
    //                 response.push(p);
    //             }
    //         }
    //     })
    // }));
    return response;
}